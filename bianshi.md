# 我现在要做一个针对飞行器动力学基本气动力系数进行辨识的工作，重点要得到系统的clcd随迎角变化规律。要让飞机怎么飞来实…

> 我现在要做一个针对飞行器动力学基本气动力系数进行辨识的工作，重点要得到系统的clcd随迎角变化规律。要让飞机怎么飞来实现。

<details><summary>1 previous message</summary>

> 你这个目标很像“先把静态极曲线摸清楚，再决定要不要做动态导数辨识”。我先按飞行试验/系统辨识的思路给你一个可执行的飞行方案框架。

</details>

要得到 `CL, CD` 随迎角 `alpha` 的规律，飞机最好飞“多组准定常迎角点”，再辅以小幅激励。核心不是做很花的机动，而是让每个迎角点上的力平衡尽量干净。

最推荐的飞法是：

1. **平直定常速度扫点**

   - 固定构型：襟翼、起落架、重心、重量范围尽量固定。
   - 在安全高度，选一组目标速度或目标迎角。
   - 每个点让飞机保持：平直、定高、定航向、侧滑接近 0。
   - 配平后保持 10-30 秒，记录 `alpha, V, h, rho, theta, q, nz, delta_e, throttle` 等。
   - 这组数据对 `CL(alpha)` 很有用，因为近似有：
     ```text
     CL = W / (0.5 * rho * V^2 * S)
     ```
   - 但 `CD` 需要知道发动机/螺旋桨推力，所以单靠平飞辨识阻力会受推力模型影响。
2. **怠速/关小油门稳定滑翔扫点**

   - 这是辨识 `CD(alpha)` 和 `CL/CD(alpha)` 最干净的飞法之一。
   - 飞机爬到安全高度后，油门固定为怠速或固定小推力。
   - 依次保持不同目标速度/迎角，做稳定下滑段。
   - 每个点保持稳定下滑 10-30 秒。
   - 若推力可忽略，准定常滑翔时：
     ```text
     L = W cos(gamma)
     D = W sin(|gamma|)
     CL = W cos(gamma) / (0.5 * rho * V^2 * S)
     CD = W sin(|gamma|) / (0.5 * rho * V^2 * S)
     CL / CD = cot(|gamma|)
     ```
   - 这里 `gamma` 是飞行路径角，可由 GPS/INS 的水平速度和下降率求出。
3. **小幅升降舵激励**

   - 在几个典型配平点附近加入小幅 elevator doublet、3-2-1-1 或 chirp。
   - 幅值要小，避免进入强非线性和大姿态变化。
   - 作用是帮助区分：
     ```text
     CL = CL0 + CL_alpha alpha + CL_q q + CL_deltae delta_e
     CD = CD0 + CD_alpha alpha + CD_alpha2 alpha^2 + CD_deltae delta_e
     ```
   - 如果你只做静态曲线，激励不是必须；但如果要做系统辨识，它很有价值。
4. **高迎角段谨慎扩展**

   - 从巡航迎角开始，逐步向低速/高迎角扩展。
   - 每次只增加一点迎角，接近抖振、失速警告或控制裕度下降时停止。
   - 高迎角段不要一开始就做大动作，先用稳定慢飞或稳定滑翔点采数据。
   - 若需要更高 `CL`，可做稳态盘旋/风up turn，但这更适合扩展升力曲线，对阻力辨识会复杂一些。

一个典型试飞卡片可以这样设计：

```text
构型：clean / 固定重心 / 固定重量范围
高度：安全高度层
侧滑：beta ≈ 0
迎角点：alpha = 0, 2, 4, 6, 8, 10, 12 deg，按飞机安全包线调整

每个点：
1. 进入目标速度或目标迎角
2. 配平
3. 保持稳定 15-30 s
4. 记录数据
5. 换下一个点

先做平飞扫点，再做怠速滑翔扫点。
每个点至少重复 2-3 次，最好做相反航向以抵消风的影响。
```

最关键的结论是：
**想要 `CL(alpha)`，飞平直定常速度扫点就很好；想要 `CD(alpha)` 和 `CL/CD(alpha)`，优先做稳定滑翔扫点，因为它不强依赖推力模型。**
如果后续还要辨识完整动力学模型，再在这些配平点附近加小幅升降舵激励。

> 没有迎角传感器呢

没有迎角传感器也能做，但要把“迎角”换成**由状态估算出来的迎角**。核心关系是：

```text
alpha ≈ theta - gamma
```

其中：

```text
theta = 俯仰角，由 IMU/AHRS 得到
gamma = 飞行路径角，由速度方向得到
```

对稳定平飞：

```text
gamma ≈ 0
alpha ≈ theta - 安装偏差
```

对稳定滑翔：

```text
gamma = atan(垂直速度 / 水平速度)
alpha ≈ theta - gamma - 安装偏差
```

这里“安装偏差”包括 IMU 安装角、机身基准线和零升力线/机体轴之间的差异。这个偏差必须校准，否则 `alpha` 曲线会整体平移。

更实用的方案是：

1. **先做相对迎角辨识**

   - 不急着得到绝对 `alpha`。
   - 用：
     ```text
     alpha_rel = theta - gamma
     ```
   - 得到 `CL(alpha_rel)` 和 `CD(alpha_rel)`。
   - 这已经能看出升力斜率、失速前趋势、阻力极曲线。
2. **用配平巡航点校准零点**

   - 选一个可信的巡航状态，比如已知重量、速度、高度、平飞。
   - 由：
     ```text
     CL = W / qbar S
     ```
   - 再结合理论或风洞/CFD/经验的 `CL0, CL_alpha`，反推出该状态的迎角。
   - 用它修正 `alpha_rel` 的零偏。
3. **用 GPS/INS 算飞行路径角**

   - 需要记录：
     ```text
     theta, phi, V_ground, vertical_speed, altitude, airspeed
     ```
   - 如果有空速，最好用空速；如果只有 GPS 速度，必须做往返航向或风估计，否则风会污染 `gamma` 和阻力估算。
   - `gamma` 可近似：
     ```text
     gamma = atan(Vz / Vh)
     ```
   - 注意符号：下滑时 `gamma` 通常为负。
4. **不要用大机动初期数据**

   - 没有迎角传感器时，动态机动里的 `alpha` 估计容易受风、IMU延迟、加速度、滤波影响。
   - 所以前期最好只用：
     ```text
     稳定平飞点
     稳定滑翔点
     稳定慢飞点
     ```
   - 每个点保持 15-30 秒，取中间稳定段平均。
5. **如果有条件，加一个临时迎角标定装置**

   - 哪怕不是正式传感器，也可以用：
     ```text
     机头小风标
     简易差压迎角探针
     外置数据采集小模块
     ```
   - 它不一定长期使用，但能帮你把 `theta - gamma` 的零偏和比例关系标定出来。

所以，没有迎角传感器时，推荐飞法仍然是：

```text
平飞速度扫点 + 怠速稳定滑翔扫点
```

但数据处理时用：

```text
alpha_est = theta - gamma - alpha_bias
```

而不是直接测量迎角。

最重要的一句：
**没有迎角传感器时，不要一上来做剧烈激励辨识 `CL/CD(alpha)`；先做准定常飞行，用姿态角减飞行路径角估算迎角。**

> 我是使用半物理拟真实现该过程，通过飞控记录数据。我是不是应该先对照迎角数据

是的，**强烈建议先对照迎角数据**。半物理拟真里通常能拿到“真值迎角”或模型内部的 `alpha`，这正好可以用来验证你后面准备用飞控记录数据反算迎角、`CL/CD` 的方法是否靠谱。

推荐顺序是：

1. **先记录仿真真值**
   同时记录：

   ```text
   alpha_true
   theta
   gamma
   V
   Vx, Vy, Vz
   qbar
   rho
   ax, az
   nz
   elevator
   throttle / thrust
   CL_true, CD_true  如果模型能输出
   ```
2. **用飞控数据反算迎角**

   对纵向小侧滑情况，可以先用：

   ```text
   alpha_est = theta - gamma
   ```

   其中：

   ```text
   gamma = atan(Vz / Vh)
   ```

   然后把 `alpha_est` 和 `alpha_true` 对比，看是否只是存在一个固定偏差：

   ```text
   alpha_true ≈ alpha_est - alpha_bias
   ```

   如果误差主要是常值偏差，那很好处理；如果误差随速度、机动、风变化明显，就说明你的 `gamma`、速度坐标系或滤波延迟还有问题。
3. **检查定义是否一致**

   这一步很重要。很多误差不是算法错，而是定义不一致：

   ```text
   alpha = atan(w / u)
   ```

   这里的 `u, w` 应该是**机体系相对气流速度分量**，不是地速分量。

   如果仿真有风，必须用空速矢量：

   ```text
   V_air = V_ground - V_wind
   ```

   否则 `theta - gamma` 算出来的是相对地面的飞行路径角，会被风污染。
4. **先用准定常段验证**

   不要一开始就用大幅激励数据校验。先选：

   ```text
   平飞定速
   稳定爬升/下滑
   慢速稳定飞行
   小迎角到中迎角扫点
   ```

   每个点取稳定段平均。你应该能看到：

   ```text
   alpha_est 和 alpha_true 基本线性一致
   CL 随 alpha 近似线性
   CD 随 alpha 呈二次型趋势
   ```
5. **再进入辨识**

   迎角估算验证通过后，再用飞控记录数据计算：

   ```text
   CL = L / (qbar S)
   CD = D / (qbar S)
   ```

   或者建立参数模型：

   ```text
   CL = CL0 + CL_alpha alpha + CL_q q_hat + CL_deltae delta_e

   CD = CD0 + k CL^2
   ```

   或：

   ```text
   CD = CD0 + CD_alpha alpha + CD_alpha2 alpha^2
   ```

我的建议是：
**在半物理拟真阶段，先把 `alpha_true` 当作标尺，验证你用飞控数据得到的 `alpha_est = theta - gamma`。验证通过后，再把真实迎角隐藏掉，只用飞控记录量跑完整套 `CL/CD` 辨识流程。**

这样你能提前发现坐标系、符号、滤波延迟、风场、安装偏差这些问题。等上真机或更真实的试验环境时，心里会踏实很多。
